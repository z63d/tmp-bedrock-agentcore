{
  "name": "bedrock-agentcore-ts",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "docker:build": "docker build -t bedrock-agent .",
    "docker:run": "docker run -p 8080:8080 bedrock-agent"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore": "^3.1000.0",
    "@strands-agents/sdk": "^0.1.5",
    "bedrock-agentcore": "^0.2.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
