display_information:
  name: "(experimental) SRE Agent"
  description: Bedrock AgentCore SRE agent (experimental)
  background_color: "#1a1a2e"

features:
  bot_user:
    display_name: "SRE Agent"
    always_online: true

oauth_config:
  scopes:
    bot:
      - app_mentions:read
      - chat:write

settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
